1) installing conda
this was a simple project to learn about how conda is used as a secure container in a work enviroment and how it can be used with working on python projects
2) coding script into vs-studio
the script itself is fetching weather from the open api, displaying it on a terminal and exporting it onto a text file
3) using api key
the api key is hardcoded itself on the project and was removed
